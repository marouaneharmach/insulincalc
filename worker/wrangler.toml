name = "clarifai-proxy"
main = "clarifai-proxy.js"
compatibility_date = "2024-01-01"

# Secrets must be set via: wrangler secret put CLARIFAI_PAT
# NEVER commit secrets to this file
[vars]
# CLARIFAI_PAT is configured as a Cloudflare secret
